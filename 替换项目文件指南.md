# 郑一鸣作品集替换指南

本项目直接基于原 `portfolio-2026` 仓库改造，页面结构、深色主题、动画、响应式导航、作品矩阵、技能树、成果轮播、特别项目详情与 Lightbox 均已保留。

目前已替换姓名、专业、求职方向、教育、技能、工程项目、获奖、专利、邮箱、电话与简历。项目图片暂时显示灰色占位。按下面的文件名放入全部图片后，将 `src/config/site.ts` 中的 `usePlaceholderImages` 改为 `false`，真实图片会统一启用。

## 1. 本地运行

```powershell
npm install
npm run dev
```

访问：`http://localhost:5173/portfolio-2026/`

## 2. 替换首页与作品集图片

推荐尺寸：1600 × 1200 px，JPG 或 WebP，单张尽量控制在 500 KB 内。

将图片放入 `public/images/works/featured/`：

- `bulk-carrier-strength.jpg`
- `bulk-carrier-stability.jpg`
- `drift-yacht.jpg`
- `blue-ring-yacht.jpg`
- `maxsurf-analysis.jpg`
- `spd-production.jpg`

将次要项目图放入 `public/images/works/grid/`：

- `propeller-design.jpg`
- `steel-platform.jpg`

对应文案位于 `src/data/works.json`。可以复制任意一个对象来增加项目，注意保持 `id` 唯一。

## 3. 替换技能配图

推荐尺寸：1600 × 1000 px。放入 `public/images/skills/`：

- `skill-structure.jpg`
- `skill-hydrostatics.jpg`
- `skill-performance.jpg`
- `skill-production.jpg`
- `skill-3d.jpg`
- `skill-render.jpg`
- `skill-analysis.jpg`
- `skill-leadership.jpg`

技能名称、说明、软件和熟练度位于 `src/data/skills.json`。

## 4. 替换深度案例图片

案例数据位于 `src/data/special-projects.ts`。每个案例包含封面、三张成果图和两张过程图。

以 DRIFT 为例，把文件放入 `public/images/special-projects/`：

- `drift-yacht-cover.jpg`
- `drift-yacht-1.jpg`
- `drift-yacht-2.jpg`
- `drift-yacht-3.jpg`
- `drift-yacht-process-1.jpg`
- `drift-yacht-process-2.jpg`

其他案例沿用数据文件里写好的路径：

- `bulk-carrier-strength-*`
- `blue-ring-yacht-*`
- `maxsurf-analysis-*`

推荐封面和详情图使用 16:9；画廊、过程图使用 4:3。图片缺失时页面会自动退回灰色占位，不会出现破图图标。

## 5. 替换个人照片与简历

- 个人照片：`public/images/profile/profile.jpg`，推荐正方形，至少 1000 × 1000 px。
- 简历：`public/resume/郑一鸣-简历.pdf`。

## 6. 修改个人信息

- 首页文字：`src/pages/home/sections/HeroBanner.tsx`
- 首页数字：`src/pages/home/sections/StatsBar.tsx`
- 教育、经历、技能标签：`src/data/about.ts`
- 关于我介绍与奖项：`src/pages/about/index.tsx`
- 联系方式：`src/pages/contact/index.tsx`
- 页尾信息：`src/app/layout.tsx`

## 7. 修改仓库名和部署路径

当前严格沿用原指南，仓库名及部署子路径为 `portfolio-2026`。如果你的 GitHub 仓库改成其他名称，需要同步修改：

- `vite.config.ts` 中的 `base`
- `src/app/router.tsx` 中的 `basename`
- `src/app/layout.tsx` 中四个页尾链接
- `src/app/router.tsx` 中 404 页面的首页链接

然后执行：

```powershell
git remote add origin https://github.com/你的用户名/portfolio-2026.git
git push -u origin main
npm run build
npm run deploy
```

## 8. 上线前必须检查

- 联系表单当前只是前端模拟提交，并不会发送邮件。
- 地图仍是占位区域。
- 检查所有项目数字、奖项名称和个人贡献是否可在面试中解释。
- 压缩所有图片，避免首屏加载过慢。
- 在手机和电脑上分别检查文字换行、图片裁切和弹窗。

## 9. 原项目归属

上游仓库保存在 Git remote `upstream`：`https://github.com/1641816031-arch/portfolio-2026.git`。

请保留上游作者声明，并在你自己的仓库中说明修改内容。当前页尾已经保留 `Based on portfolio-2026` 声明。
